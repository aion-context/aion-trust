# ── Build artifacts ──────────────────────────────────────────────
/target/

# ── Runtime data — will hold keys once code lands. Never commit. ─
/aion-trust-data/

# ── Secrets / credentials — never commit ────────────────────────
*.env
*.key
*.pem
*.p12
.creds/
!*.env.example
# Wallet files (encrypted, but still hold key material — keep them out of git)
*.wallet
*.wallet.json
/wallets/

# ── OS / editor noise ───────────────────────────────────────────
.DS_Store
*.swp
*~
mutants.out/
site/
