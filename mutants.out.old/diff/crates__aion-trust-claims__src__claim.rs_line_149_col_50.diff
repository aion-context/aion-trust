--- crates/aion-trust-claims/src/claim.rs
+++ replace != with == in Claim::verify
@@ -141,17 +141,17 @@
         let signing = signing_bytes(
             &self.subject_id,
             &self.issuer_id,
             &self.claim_type,
             &self.schema_id,
             &self.validity,
             &body_hash,
         );
-        if ClaimId::from_signing_bytes(&signing) != self.claim_id {
+        if ClaimId::from_signing_bytes(&signing) == /* ~ changed by cargo-mutants ~ */ self.claim_id {
             return Err(ClaimReject::ClaimIdMismatch);
         }
         let sig = decode_array::<64>(&self.issuer_signature).map_err(|_| ClaimReject::Malformed)?;
         issuer_vk
             .verify(&signing, &sig)
             .map_err(|_| ClaimReject::BadSignature)?;
         Ok(VerifiedClaim(self.clone()))
     }
